{
  "name": "@qef/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run --directory ../.. uvicorn apps.api.main:app --reload --port 8000",
    "start": "uv run --directory ../.. uvicorn apps.api.main:app --port 8000"
  }
}
